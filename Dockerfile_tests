FROM alpine:3.18.2

RUN apk update && \
    apk add --no-cache g++ gtest-dev

WORKDIR /app

COPY src/ ./src/
COPY tests/ ./tests/

RUN g++ -o diff-eq-solver-tests tests/main.cpp tests/*/*.cpp src/*/*.cpp -lgtest -lgtest_main -pthread

ENTRYPOINT [ "/app/diff-eq-solver-tests" ]
